[build-system]
requires = ["maturin>=1,<2"]
build-backend = "maturin"

[project]
name = "quant-microstructure-core"
version = "0.1.0"
requires-python = ">=3.12"

[tool.maturin]
module-name = "_microstructure_rs"
include = ["_microstructure_rs.pyi"]
